# Copy to .env for local development:
#   cp .env.example .env
#
# Never commit .env

GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# Optional
WALKSCORE_API_KEY=your-walkscore-api-key-here
SECRET_KEY=any-random-string-for-flask-sessions
# Set to "true" to enable builder mode (demo evaluations when key is missing,
# diagnostic panels, analytics dashboard).
BUILDER_MODE=true
