# Copy to .env for local development:
#   cp .env.example .env
#
# Never commit .env

# =============================================================================
# REQUIRED — evaluations will fail without this
# =============================================================================

# Google Maps API key (Places, Distance Matrix, Geocoding).
# Required. Without it the app starts but all address evaluations fail.
GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# =============================================================================
# OPTIONAL API KEYS — features degrade gracefully when missing
# =============================================================================

# Separate domain-restricted key for the browser Places autocomplete widget.
# Optional. Falls back to GOOGLE_MAPS_API_KEY when unset.
GOOGLE_MAPS_FRONTEND_API_KEY=

# Walk Score API key (walkscore.com). Provides walk / transit / bike scores.
# Optional. Walk scores silently return None when missing.
# WALKSCORE_API_KEY=your-walkscore-key-here

# Census Bureau API key. Provides demographic data (ACS 5-year estimates).
# Optional. Without it requests are limited to 500/day by the Census Bureau.
# CENSUS_API_KEY=your-census-api-key-here

# Sentry DSN for error tracking.
# Optional. Error tracking is silently disabled when unset.
# SENTRY_DSN=https://your-dsn@sentry.io/project-id

# =============================================================================
# APP CONFIGURATION
# =============================================================================

# Flask secret key for session signing and CSRF tokens.
# Optional. Defaults to 'nestcheck-dev-key' — set a real value in production.
# SECRET_KEY=your-random-secret-here

# Enables extra debug UI (builder dashboard) and removes internal limits.
# Optional. Default: false.
BUILDER_MODE=true

# Secret token for builder-mode access via ?builder_secret= query param.
# Optional. Default: 'nestcheck-builder-2024'.
# BUILDER_SECRET=your-builder-secret-here

# How many days a cached snapshot is considered fresh before re-evaluation.
# Optional. Default: 90.
# SNAPSHOT_TTL_DAYS=90

# Server port.
# Optional. Default: 5001 (dev server) or 8000 (gunicorn).
# PORT=5001

# Flask debug mode. Set to '1' to enable auto-reload and debugger.
# Optional. Default: 0.
# FLASK_DEBUG=1

# =============================================================================
# EVALUATION FEATURE GATES
# =============================================================================

# Schools evaluation. Disabled by default — adds ~200+ Google Maps API calls
# per evaluation. Set to 'true' to enable.
ENABLE_SCHOOLS=false

# =============================================================================
# STRIPE PAYMENT INTEGRATION (optional — app works without these in free mode)
# =============================================================================

# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# STRIPE_PRICE_ID=price_...

# Require payment for evaluations. Default: false (free mode).
REQUIRE_PAYMENT=false

# =============================================================================
# URBAN ACCESS DESTINATIONS — customize for non-NYC metro areas
# =============================================================================

# Primary commute hub address.
# Optional. Default: "Grand Central Terminal, New York, NY".
# PRIMARY_HUB_ADDRESS=Grand Central Terminal, New York, NY

# Airport hubs as JSON array of {"name": "...", "address": "..."} objects.
# Optional. Default: JFK, LaGuardia, Newark.
# AIRPORT_HUBS=[{"name":"JFK","address":"JFK Airport, Queens, NY"}]

# Downtown hub address.
# Optional. Default: "Downtown Manhattan, New York, NY".
# DOWNTOWN_HUB=Downtown Manhattan, New York, NY

# Hospital hub address.
# Optional. Default: "NewYork-Presbyterian Hospital, New York, NY".
# HOSPITAL_HUB=NewYork-Presbyterian Hospital, New York, NY

# =============================================================================
# INFRASTRUCTURE — typically only changed in deployment
# =============================================================================

# SQLite database file path.
# Optional. Default: "nestcheck.db".
# NESTCHECK_DB_PATH=nestcheck.db

# SpatiaLite spatial database path (EPA, FEMA, HPMS datasets).
# Optional. Default: "data/spatial.db". Spatial queries return empty when missing.
# NESTCHECK_SPATIAL_DB_PATH=data/spatial.db

# Railway volume mount path (Railway.app deployment only).
# When set, spatial.db is loaded from this volume instead of NESTCHECK_SPATIAL_DB_PATH.
# RAILWAY_VOLUME_MOUNT_PATH=/mount

# Overpass API interpreter URL.
# Optional. Default: "https://overpass-api.de/api/interpreter".
# OVERPASS_BASE_URL=https://overpass-api.de/api/interpreter

# Overpass API status endpoint (used by health monitor).
# Optional. Default: "https://overpass-api.de/api/status".
# OVERPASS_STATUS_URL=https://overpass-api.de/api/status

# Seconds between background health checks (Overpass, Open-Meteo).
# Optional. Default: 300 (5 minutes).
# HEALTH_CHECK_INTERVAL=300

# =============================================================================
# RATE LIMITING (optional — sensible defaults are built in)
# =============================================================================

# RATE_LIMIT_DEFAULT=60/minute
# RATE_LIMIT_EVAL=10/hour
